# 🧪 Руководство по тестированию MS Clarity

## 📋 Как проверить, что Clarity работает

### 1️⃣ Откройте браузер с Developer Tools

1. Откройте страницу в браузере
2. Нажмите **F12** или **Ctrl+Shift+I** (Windows) / **Cmd+Option+I** (Mac)
3. Откройте вкладку **Console**

### 2️⃣ Проверьте инициализацию Clarity

В консоли должны появиться сообщения:
```
✅ Clarity script loaded
```

Также проверьте в консоли:
```javascript
window.clarity
```
Должна вернуться функция, а не `undefined`

### 3️⃣ Тестирование кликов

Нажмите на каждую кликабельную зону и проверьте логи в консоли:

| # | Элемент | Ожидаемый лог | data-clarity-click |
|---|---------|---------------|-------------------|
| 1 | **Логотип "Chile Access"** | `✅ Clarity tracked: cta_logo_chile_access` | `cta_logo` |
| 2 | **Тег "🔥 Bono activo"** | `✅ Clarity tracked: cta_tag_exclusivo_hoy` | `cta_exclusivo` |
| 3 | **Эмодзи 🎰** | `✅ Clarity tracked: cta_main_continuar` | `cta_slot_emoji` |
| 4 | **"🎁 Reclama tus giros..."** | `✅ Clarity tracked: cta_main_continuar` | `cta_gift_line` |
| 5 | **Кнопка "Reclamar Bono"** | `✅ Clarity tracked: cta_main_continuar`<br>`✅ Clarity tracked redirect: redirect_unknown` | `cta_main` |
| 6 | **"💰 8.472 jugadores..."** | `✅ Clarity tracked: cta_main_continuar` | `cta_social_proof` |
| 7 | **"Juega ahora · Retiros..."** | `✅ Clarity tracked: cta_main_continuar` | `cta_benefits` |
| 8 | **Футер (весь блок)** | `✅ Clarity tracked: cta_main_continuar` | `cta_footer` |

### 4️⃣ Проверка в MS Clarity Dashboard

1. Откройте https://clarity.microsoft.com/
2. Выберите проект **ufq3clrf2z**
3. Перейдите в **Recordings** (Записи)
4. Найдите свою последнюю сессию
5. Проверьте, что все клики записаны

В разделе **Heatmaps** проверьте:
- Тепловая карта кликов показывает все 8 зон
- Все кнопки имеют активность

### 5️⃣ Проверка Custom Events

В Clarity Dashboard → **Dashboards** → **Custom Tags** должны появиться события:

**События кликов:**
- `cta_logo_chile_access` = true
- `cta_tag_exclusivo_hoy` = true
- `cta_main_continuar` = true

**События редиректов:**
- `redirect_unknown` = true

### 6️⃣ Если Clarity не загружается

Если видите предупреждение:
```
⚠️ Clarity not loaded yet for: cta_xxx
```

**Причины:**
1. Блокировщик рекламы (AdBlock, uBlock Origin) - отключите
2. Медленное интернет-соединение
3. Clarity заблокирован файрволом

**Решение:**
- Скрипт автоматически повторит попытку через 500ms
- Должен появиться лог: `✅ Clarity tracked (delayed): cta_xxx`

### 7️⃣ Проверка сетевых запросов

Во вкладке **Network** (Сеть) в DevTools:

1. Очистите логи (кнопка 🚫)
2. Перезагрузите страницу
3. Найдите запрос к: `https://www.clarity.ms/tag/ufq3clrf2z`
4. Статус должен быть **200 OK**

### 8️⃣ Тест редиректа (ВАЖНО!)

⚠️ **Внимание:** При клике на любую кликабельную зону происходит редирект на:
```
https://fresh-blrs10.com/c8212cb08
```

Чтобы протестировать БЕЗ редиректа:

1. Откройте `/components/PrelandingPage.tsx`
2. Временно закомментируйте строку:
   ```typescript
   // window.location.href = LANDING_URL;
   ```
3. Сохраните и протестируйте клики
4. Все логи должны появляться в консоли
5. **НЕ ЗАБУДЬТЕ** вернуть редирект обратно!

## 🎯 Чеклист успешного теста

- [ ] В консоли нет ошибок
- [ ] `window.clarity` возвращает функцию
- [ ] При клике на логотип: логируется `cta_logo_chile_access`
- [ ] При клике на тег: логируется `cta_tag_exclusivo_hoy`
- [ ] При клике на кнопку: логируется `cta_main_continuar` + `redirect_unknown`
- [ ] При клике на остальные зоны: логируется `cta_main_continuar`
- [ ] В Clarity Dashboard появляются записи сессий
- [ ] На фон и обычный текст клик НЕ работает (нет редиректа)

## 🐛 Частые проблемы

### Проблема: "window.clarity is undefined"
**Решение:** Подождите 2-3 секунды после загрузки страницы

### Проблема: "Clarity tracked (delayed)"
**Решение:** Это нормально! Скрипт загружается асинхронно

### Проблема: Клик не работает на мобильном
**Решение:** Проверьте, что `touch-manipulation` применен к кнопке

### Проблема: Клик работает везде, а не только на кнопках
**Решение:** Убедитесь, что убран глобальный обработчик `onClick` с главного div

## 📊 Ожидаемые метрики в Clarity

После 10-20 тестовых кликов в Clarity должно быть видно:

- **Click Rate**: Высокий процент кликов на главную CTA
- **Rage Clicks**: Минимум (если есть - значит что-то не работает)
- **Dead Clicks**: Только на некликабельных зонах (это нормально)
- **Engagement Time**: 5-15 секунд (быстрый редирект)

## ✅ Готово к продакшену если:

1. ✅ Все 8 кликабельных зон логируют события
2. ✅ Фон и текст НЕ кликабельны
3. ✅ Редирект работает мгновенно
4. ✅ В Clarity видны все сессии
5. ✅ Нет ошибок в консоли
6. ✅ PageSpeed > 90/100
