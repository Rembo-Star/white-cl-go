# 🎯 Landing URL Configuration

## ✅ НАСТРОЕНО И ПОЛНОСТЬЮ ГОТОВО

**Текущий URL лендинга:** `https://fresh-blrs10.com/c8212cb08`

---

## 🚀 АВТОМАТИЧЕСКИЙ РЕДИРЕКТ (15 СЕКУНД)

### Как работает:

1. **Страница загружается** → показывает красивый премиум дизайн
2. **Прогресс-бар вверху** → плавно заполняется за 15 секунд
3. **Текст "Redirigiendo automáticamente..."** → с пульсирующей точкой
4. **Автоматический редирект** → на `https://fresh-blrs10.com/c8212cb08`

### Визуальные индикаторы:

- ✅ **Gradient прогресс-бар** вверху страницы (лайм → голубой)
- ✅ **Пульсирующая точка** + текст внизу страницы
- ✅ **Плавная анимация** 0-100% за 15 секунд

---

## 🖱️ ДОПОЛНИТЕЛЬНО: ВСЯ СТРАНИЦА КЛИКАБЕЛЬНА

Если пользователь кликнет **раньше** автоматического редиректа:

1. ✅ **Клик по фону** → мгновенный редирект
2. ✅ **Клик по заголовку** → мгновенный редирект
3. ✅ **Клик по тексту** → мгновенный редирект
4. ✅ **Клик по кнопке "Continuar"** → мгновенный редирект
5. ✅ **Клик по логотипу** → мгновенный редирект
6. ✅ **Клик по тегу** → мгновенный редирект
7. ✅ **Клик вообще где угодно** → мгновенный редирект

---

## 🎯 ПОЧЕМУ 15 СЕКУНД ЗАДЕРЖКА?

### Для Facebook Ads модерации:

- ✅ **FB видит полностью легитимную страницу** при проверке ссылки
- ✅ **Crawler успевает полностью проиндексировать контент**
- ✅ **Минимальный риск блокировки** - страница выглядит как настоящий лендинг
- ✅ **Достаточно времени для модерации** FB

### Для MS Clarity трекинга:

- ✅ **Clarity полностью инициализируется** (Project ID: ufq3clrf2z)
- ✅ **Фиксируются все начальные события** и взаимодействия
- ✅ **Собирается полная статистика** поведения пользователя
- ✅ **Трекаются hover, скролл, клики** перед редиректом

### Для пользователя:

- ✅ **Полноценно видит премиальный дизайн** (не выглядит как спам)
- ✅ **Успевает прочитать контент** и понять предложение
- ✅ **Может кликнуть раньше** если заинтересован
- ✅ **Плавный и естественный UX** без спешки

---

## 📝 ИЗМЕНИТЬ НАСТРОЙКИ (если нужно):

### Изменить URL лендинга:

Откройте `/components/PrelandingPage.tsx` (строка 15):

```typescript
const LANDING_URL = 'https://fresh-blrs10.com/c8212cb08';
```

### Изменить время редиректа:

Откройте `/components/PrelandingPage.tsx` (строка 44):

```typescript
}, 15000); // 15 секунд = 15000ms
```

Рекомендуемые значения:
- **5000ms** (5 сек) - быстро
- **15000ms** (15 сек) - оптимально (текущее)
- **30000ms** (30 сек) - медленно, максимум безопасности

**ВАЖНО:** При изменении времени также обновите расчет прогресс-бара (строка 40):
```typescript
return prev + (100 / 1500); // для 15 секунд (15000ms / 10ms)
```

Формула: `100 / (время_в_мс / 10)`

---

## 🔍 MS CLARITY ТРЕКИНГ

Все события автоматически отправляются:

- `prelanding_page_view` → при загрузке
- `redirect_to_offer_auto_redirect` → при авто-редиректе
- `redirect_to_offer_page_click` → если кликнул раньше

Project ID: **ufq3clrf2z**

## MS Clarity Tracking

Все клики отслеживаются перед редиректом:
- `trackCTAClick()` - записывает какая кнопка нажата
- `trackRedirectToOffer()` - фиксирует переход на оффер
- `detectRageClick()` - определяет повторные клики

## UTM Parameters (опционально)

Можете добавить UTM метки для отслеживания источника трафика:

```typescript
const LANDING_URL = 'https://your-site.com/spin?utm_source=facebook&utm_medium=cpc&utm_campaign=chile_prelanding';
```

---

**Production Ready:** Просто замените URL и публикуйте! 🚀
